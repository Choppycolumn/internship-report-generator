# 第一阶段测试与视觉检查报告

日期：2026-07-10

## 输入导入结果

- 《使用注意事项》SHA-256：`f1b33f28e8f2866385fb2631e64a06034ab7b8c9655c22fc64d479d6d5c41e38`。
- 归类：`school_requirement_only`；`may_be_used_as_template=false`。
- 模板候选 PDF SHA-256：`f7881a3e3bc3cb89d952d7d1b6b95e2120f69fbb9a4d4b4abb637627b2c8de0e`。
- 模板候选状态：`awaiting_physical_size`。
- PDF 诊断介质框：168.981 × 241.653 mm；未写入正式物理尺寸字段。
- 内嵌扫描图：3990 × 5702 像素；无可信 DPI。

## 自动测试

执行命令：

```powershell
.venv\Scripts\python -m pytest
```

结果：`16 passed`。

覆盖项包括：

- 毫米/像素和毫米/PDF 点坐标往返。
- 接近 A4 比例或带 DPI 的图像不得推断物理尺寸。
- 用户显式尺寸设置和状态转换。
- 缺少物理尺寸时，校正和预览被门禁阻止。
- 明确全画幅扫描的比例校正。
- 手工四角透视校正（TL、TR、BR、BL）。
- 禁打区碰撞、页面越界和过小字号阻止正式输出。
- 打印偏移和以页面中心为基准的缩放。
- 三类 PDF 的精确非 A4 尺寸。
- 预览 PDF 包含背景图像，正式套打 PDF 不包含背景图像。

额外自动检查 `scripts/run_phase1_checks.py` 结果：全部通过。

## PDF 物理尺寸检查

测试夹具是明确标注的 120 × 180 mm 合成页面，不代表任何真实报告纸。

| 文件 | 检查尺寸 | 背景图像对象 |
| --- | --- | --- |
| 预览 PDF | 120.0000 × 180.0000 mm | 1 |
| 正式套打 PDF | 120.0000 × 180.0000 mm | 0 |
| 调试 PDF | 120.0000 × 180.0000 mm | 1 |
| 打印校准 PDF | 120.0000 × 180.0000 mm | 0 |

渲染器：PDFium；渲染像素为 681 × 1021。

## 视觉检查

第一次检查发现调试 PDF 使用 Helvetica 绘制中文区域标签，出现彩色方块。已改为中文字体并重新生成全部 PDF。

第二次检查结果：

- 预览页：背景、固定框线、横线和新增中文均清晰，无裁切和乱码。
- 正式套打页：只有两行新增文字，无扫描背景、固定框线或横线。
- 调试页：10 mm 网格、正文区、页眉区、装订禁打区、真实横线、文本框和基线均可辨认；中文标签正常。
- 校准页：四角十字、中心十字、10 mm 网格、关键横线和打印说明完整，无越界。

## 当前明确未完成或待输入

- 真实模板缺少实测宽高，因此没有生成真实模板的预览或正式套打 PDF。
- OpenCV 自动四角识别代码已实现，但当前环境下载 OpenCV wheel 超时；已实际测试无需 OpenCV 的手工四角透视和全画幅回退路径。
- Streamlit 标定界面代码已完成并通过编译；当前环境未安装其大型依赖链，尚未进行浏览器端交互测试。
- 横线自动检测、中文逐行换行、图文混排、每日资料生成和 8000 字完整报告属于后续阶段，不在本阶段冒充完成。

